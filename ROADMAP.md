# ロードマップ

今後行う作業だけを記載する。完了した内容は `CHANGELOG.md` へ移す。

## 1. v0.1.0 リリース

- macOSで、アプリ起動、PDF・画像読込、初期設定、名簿読込、記述採点、
  集計出力、セッション復元を手動確認する。
- GitHub ActionsのmacOSテストを通す。
- README、ライセンス、クレジット、旧名称の残存を最終確認する。
- `CHANGELOG.md` の内容とバージョン定数が `0.1.0` に対応していることを確認する。
- `v0.1.0` タグとGitHub Releaseを公開する。

## 2. GUIの仕上げ

- 小型画面や表示倍率変更時にも主要な操作が隠れないようにする。
- 全主要ダイアログへ共通のウィンドウサイズ・位置調整を適用する。
- 答案の画像一覧・テキスト一覧、横スクロール、注釈編集をmacOS実機で確認する。
- 採点中に別ウィンドウを行き来する回数を減らす。

### トップ画面とワークフローの改良（未完了）

トップ画面で、作業の状態と次に行う操作が一致し、案件の種類（画像／PDF／複数ページ）を
意識せずに順番どおり進められる状態にする。

- [x] 作業スペース選択、ページ数確定、答案ファイル追加、採点準備、採点実行、集計の工程をトップ画面に表示する。
- [x] 現在の状態に応じて「次にすること」と案内ボタンを切り替える。
- [x] PDF案件では、採点準備より先に答案ファイル追加を案内する。
- [x] 答案取込画面を閉じた後、トップ画面の工程状態を再評価する。
- [x] 答案取込済みのPDF案件で、トップ画面から試験ページ選択・採点準備へ確実に遷移できるようにする。
- [x] 答案ファイル追加後に、未取込・取込済み・採点準備済みをトップ画面で明確に表示する。
- [x] 画像案件とPDF案件で、不要な工程ボタンや誤解を招く案内を表示しない。
- [x] 各工程のボタン活性条件と実際の処理状態を統一し、状態更新漏れをなくす。
- [x] 途中で画面を閉じたりアプリを再起動した場合も、最後に完了した工程から再開できるようにする。
- [ ] 作業を開始できない場合、原因（ページ数未確定、答案未取込、学籍番号未確認など）を画面上で具体的に示す。
  （ページ切替エラーの文言は改善済み。学籍番号未確認の事前チェックは未着手。）
- [ ] 画像案件・単一ページPDF・複数ページPDFの主要導線を自動テストで確認する。
  （今回の変更点に対するテストは追加済み。既存フロー全体の網羅的な自動テストは未着手。）
- [ ] macOS実機で、答案追加から採点開始までの一連の操作を手動確認する。

## 3. 将来候補

### 複数ページ答案（着手中）

PDF・画像を試験ページごとの取込バッチとしてアプリへ追加し、学籍番号で
同一生徒の答案を関連付ける。ページ不足、重複、未関連付けを採点・集計前に
検出できるようにする。同じ試験ページが複数PDFに分かれる運用にも対応する。

- [x] 取込バッチ、答案ページ、取込元PDF・PDF内ページのデータモデルを追加する。
- [x] 同じ試験ページへ複数PDFを追加できるようにする。
- [x] 確認済み学籍番号から「学籍番号 × 試験ページ」の関連付け表を構築する。
- [x] ページ不足、同一ページ内の重複、未関連付け、名簿外IDを監査する。
- [x] 取込バッチと監査結果をセッション復元可能なJSON形式で保存・復元する。
- [x] 試験ページの追加と、PDF・画像の追加をGUIへ接続する。
- [x] 取込バッチ単位で試験ページを変更・削除できるようにする。
- [x] 共通レイアウト時に学籍番号欄・氏名欄・解答欄の初期設定を全ページへ適用する。
- [x] トップ画面にページ数・現在ページ・ページ別進捗と前後移動を表示する。
- [x] 全ページの集計完了後、学籍番号をキーに統合サマリーを自動生成する。
- [ ] 個別答案単位で試験ページを変更・除外できるようにする。
- [ ] 各ページの学籍番号OCR・確認画面を順番に実行する。
- [ ] 監査結果を一覧表示し、問題がある答案を修正できるようにする。
- [ ] 採点結果確認と答案PDF出力を全ページ横断表示へ接続する。

### 採点画面の統合

「1枚ずつ採点」と「一覧採点」を同じ画面内で切り替えられるようにし、
通常の採点作業で同時に開くウィンドウを減らす。
