# ロードマップ（今後実装したいこと）

まだ着手していないが、実装したい機能・修正をここに記録する。
着手したら該当セクションの先頭に `[着手中]` を付け、実装が終わったら
このファイルから削除して構わない。

---

## 2. [着手中] 採点補助機能（採点基準メモ・一言コメント・採点保留タグ）

> 進捗（2026-07-28）: Phase Aを開始。`descriptive_annotations.json` の
> 保存・読込・正規化基盤と、採点基準メモの設定・採点中編集・両採点モードでの
> 参照まで実装済み。Phase Bの「1枚ずつ」採点画面に、教員用メモ、
> 生徒向けコメントと履歴、保留、複数タグ、自動保存を実装済み。
> 次は一覧モードへの注釈編集・バッジ・フィルタ統合を行う。
>
> 操作改善（2026-07-28）: 数字キー採点の初期フォーカスを修正し、
> 「保留して次へ」ボタンを追加。画像フォルダ／PDF選択後は画像準備から
> 初期設定まで自動で進み、既存設定の初期化確認は各設定を始める前に行う。

### 2-0. 目的と実装方針

記述式採点中の判断根拠を残し、似た答案を比較し、採点済み答案へ必要な
フィードバックを返せるようにする。対象は `descriptive_gui.py` の「1枚ずつ」
および「一覧（グリッド）」の両採点モードと、採点結果の確認画面、
`descriptive_renderer.py` の答案描画である。

既存の `descriptive_scores.json` は、CTT分析・Excel出力・R連携などが
`{画像ファイル名: {問題ID: 整数得点}}` を前提としている。この形式は変更せず、
追加情報は新しい `descriptive_annotations.json` に分離する。これにより、既存データ、
既存の分析処理、旧バージョンとの後方互換性を維持する。

実装は次の順序で進める。

1. 共通データモデルと保存処理
2. 採点基準メモ
3. 回答ごとのメモと一言コメント
4. 採点保留とタグ
5. 確認画面・答案描画・回帰テスト

### 2-1. データモデルと永続化

#### 問題設定（`descriptive_config.json`）

設問ごとの採点基準は既存の問題設定に任意キーとして追加する。

```json
{
  "version": 2,
  "questions": [
    {
      "id": "D1",
      "name": "記述1",
      "max_score": 5,
      "aspect": 1,
      "region": [100, 200, 500, 300],
      "rubric_memo": "結論2点、理由3点。単位不足は1点減点。"
    }
  ]
}
```

- `rubric_memo` はプレーンテキストとし、空文字を許可する。
- キーがない旧設定は空文字として扱う。読み込み時にファイルを書き換えない。
- 問題の追加・編集の両画面から変更できるようにする。
- 問題を削除する際は、対応する注釈とコメント履歴も削除対象として確認する。

#### 回答注釈（新規 `descriptive_annotations.json`）

```json
{
  "version": 1,
  "answers": {
    "001.jpg": {
      "D1": {
        "memo": "別解。後で他答案と比較する。",
        "comment": "理由の説明が不足しています。",
        "held": true,
        "tags": ["別解", "境界答案"]
      }
    }
  },
  "comment_history": {
    "D1": [
      "理由の説明が不足しています。",
      "単位を書きましょう。"
    ]
  }
}
```

- `memo` は教員用であり、答案画像・Excel・分析結果には出力しない。
- `comment` は生徒へ返す一言コメントで、答案画像へ出力する。
- `held` は採点保留状態。得点の有無とは独立させる。
- `tags` は自由入力、複数可、同一回答内では重複不可。前後空白を除去し、空タグは保存しない。
- `comment_history` は問題ごとに独立させ、重複を除き、最近使用した順に並べる。
  初期上限は50件とし、上限値は定数化する。
- すべて既定値（空文字、`false`、空配列）の回答は保存時に削除し、ファイルの肥大化を防ぐ。
- 注釈ファイルが存在しない場合は空データとして開始し、旧セッションをそのまま開けるようにする。
- 不正JSONからの復旧、アトミック保存、`.bak` からの復元は既存の
  `atomic_json_save()` / `load_json_safe()` を利用する。

#### 実装箇所

- `descriptive_scorer.py`
  - `DESCRIPTIVE_ANNOTATIONS_FILE` 定数を追加する。
  - `load_descriptive_annotations()` / `save_descriptive_annotations()` を追加する。
  - 旧ファイルや任意キー欠損を正規化する小さな純粋関数を用意する。
- `descriptive_gui.py`
  - `DescriptiveScorerGUI` が得点と注釈を同時に読み込み、各採点画面へ渡す。
  - 得点の保存タイミングと同じ箇所で注釈も保存する。
  - テキスト入力はフォーカス移動時と画面終了時に確定し、入力中は300〜500msの
    デバウンス付き自動保存を行う。クラッシュ時の損失を現在編集中の数文字に限定する。
  - 「キャンセル」は画面を閉じる意味とし、得点と同様に、それまでの注釈は保存する。
- `main_gui.py`
  - セッションの完全性確認では注釈ファイルを必須にしない。
  - 採点済み答案生成時に注釈を読み込み、描画関数へ渡す。

### 2-2. 採点基準メモ

#### UI仕様

- 記述問題設定画面の問題追加・編集欄に「採点基準メモ」を追加する。
- 1枚ずつの採点画面では答案画像の横に編集可能なメモ欄を常時表示する。
- 長文を想定して複数行・折り返し・縦スクロールに対応する。
- 採点領域を圧迫しないよう、初期表示は3〜5行程度とし、「折りたたむ／展開する」を用意する。
- 答案を見ながら随時追記できるようにし、入力停止後に自動保存する。
- 一覧モードでは上部バーから編集ダイアログを開き、各カードへ同じ文面を重複表示しない。

#### 受け入れ条件

- 設問ごとのメモを登録・編集・削除し、再起動後も復元できる。
- 旧 `descriptive_config.json` を警告なしで読み込める。
- 1枚ずつ／一覧の両モードで、同じ最新のメモを確認できる。
- 採点基準メモは採点済み答案には印字されない。

### 2-3. 回答ごとのメモ

#### UI仕様

- 1枚ずつモードの画像下部または右側に「教員用メモ」欄を配置する。
- 前後移動、未採点ジャンプ、フィルタ変更、画面終了の前に現在値をモデルへ反映する。
- 一覧モードでは、カードのメモアイコンから小さな編集ダイアログを開く。
- メモがあるカードはアイコンまたはバッジで識別できるようにする。
- 採点結果確認画面でも内容の閲覧・編集を可能にする。

#### 受け入れ条件

- 画像ファイル名と問題IDの組み合わせごとに独立して保存・復元できる。
- 画面移動直後や採点画面を閉じた直後にも内容が失われない。
- メモの有無で得点、進捗判定、分析、Excel出力、答案描画が変化しない。

### 2-4. 一言コメント機能

#### UI仕様

- 1枚ずつモードに「生徒へのコメント」入力欄と履歴候補のコンボボックスを置く。
- 候補選択で入力欄へ設定でき、新しい文面は確定時にその問題の履歴へ追加する。
- 一覧モードでは各カードのコメントアイコンから選択・編集できるようにする。
- コメントありのカードにはバッジを表示する。
- 履歴候補の個別削除と、その問題の履歴全消去を可能にする。
- コメントは1回答につき1件とする。複数の指摘は改行または区切り文字で入力する。

#### 答案描画

- `draw_descriptive_on_image()` に回答注釈を任意引数として渡せるようにする。
- コメントは対象解答欄の直下を第一候補とし、用紙外にはみ出す場合は欄内下端へ配置する。
- 得点表示と重ならないよう、コメント用の矩形を先に決定してから得点の有効領域を調整する。
- 日本語フォント、改行、折り返し、出力倍率を考慮してフォントサイズを自動調整する。
- 長すぎて収まらない場合は、最小フォントまで縮小したうえで末尾を省略記号にする。
- コメントは黒または濃紺、半透明の白背景付きとし、採点記号との視認性を分ける。
- 描画詳細設定に `descriptive_show_comment`（既定値 `true`）を追加し、出力時に非表示を選べるようにする。
- コメント未設定の場合は従来とピクセル単位で同等の描画結果を維持する。

#### 受け入れ条件

- コメント候補は設問間で混ざらず、再起動後も使用できる。
- 自由入力したコメントが履歴へ追加され、重複登録されない。
- コメントの表示ON/OFFがセッション保存・復元後も維持される。
- 低解像度・高解像度の出力で、コメントが用紙外や対象欄外へ不自然にはみ出さない。
- 教員用メモ、保留タグは答案画像へ印字されない。

### 2-5. 採点保留タグ

#### 状態と操作

- 「保留」は得点とは独立した状態にする。未採点のまま保留、仮得点を付けて保留、
  採点確定後に保留解除のすべてを許可する。
- 1枚ずつモードに「保留」トグルとタグ入力欄を置く。
- タグは Enter またはカンマ区切りで追加し、チップ表示から個別削除できるようにする。
- タグを追加しただけでは自動的に保留にしない。保留解除時もタグは比較履歴として残し、
  明示的に削除できるようにする。
- キーボードショートカット（候補: `h`）を設ける。ただし既存の得点入力、
  Entry/Textへの文字入力中には発火させない。

#### 一覧・絞り込み

- 問題一覧の進捗に「採点済み数」と「保留数」を併記する。
- 1枚ずつモードに「保留のみ」と「タグ」のフィルタを追加する。
- 一覧モードでは保留カードを橙色の枠とバッジで強調し、タグを表示する。
- 「保留のみ」「指定タグ」「未採点のみ」を組み合わせて絞り込めるようにする。
- 保留答案の間を前後移動できるようにする。
- 採点完了時に保留が残っていれば件数を示して確認する。ただし保存・終了自体は妨げない。

#### 受け入れ条件

- 同じ回答へ複数タグを追加・削除でき、重複や空タグが保存されない。
- 得点変更や得点リセットで、保留状態・タグが意図せず失われない。
- 問題設定から「この問題の採点をリセット」を実行する際は、得点だけを消すか、
  注釈も含めて消すかを明示的に選べる。
- 保留中でも仮得点は分析・Excel出力に従来どおり含める。保留情報自体は分析値を変えない。
- 保留・タグ・教員用メモは採点済み答案へ印字されない。

### 2-6. 採点結果確認画面との統合

- 既存の記述採点結果確認画面に、メモ・コメント・保留・タグの状態を示す列またはバッジを追加する。
- 問題、得点、保留状態、タグ、コメント有無で絞り込めるようにする。
- 確認画面から得点と注釈を編集し、両JSONをアトミックに保存する。
- 保留答案を一括確認できる導線を問題一覧画面にも置く。
- ファイル名変更や画像の追加・削除で参照不能になった注釈は自動削除せず、ログへ警告する。
  将来の再関連付けに備え、明示的なクリーンアップ操作を別途検討する。

### 2-7. テスト計画

#### 単体テスト

- 注釈JSONの保存・読込・`.bak` 復旧・欠損キー補完。
- 空注釈の削除、タグの空白除去・重複排除、コメント履歴の順序と上限。
- 旧問題設定・旧得点データを変更せず読み込めること。
- 保留数・採点済み数・フィルタ結果を算出する純粋関数。
- コメント描画の座標計算、折り返し、省略、表示ON/OFF。

#### GUIテスト

- 1枚ずつモードでの回答切替時の保存。
- 一覧モードのバッジ、編集ダイアログ、複合フィルタ。
- Text/Entry入力中に得点・保留ショートカットが誤発火しないこと。
- 問題設定画面での採点基準メモ編集と、採点画面への反映。
- 採点完了時の保留警告と、終了継続・キャンセルの両分岐。

#### 統合・回帰テスト

- アプリ再起動を挟んだ得点・注釈・コメント履歴の復元。
- 旧セッション、注釈なしセッション、破損注釈ファイルからの起動。
- CTT分析、Excel出力、R連携の数値が追加前と一致すること。
- コメントあり／なし、複数出力倍率、日本語長文での答案画像スナップショット。
- macOSのTk環境で、1枚ずつ／一覧／確認画面の手動スモークテスト。

追加するテストファイルの目安:

- `tests/test_descriptive_annotations.py`: データモデルと永続化
- `tests/test_descriptive_annotation_gui.py`: GUI状態遷移と保存
- `tests/test_descriptive_comment_rendering.py`: コメント描画
- 既存の `test_session_state.py`、`test_rendering_settings.py`、
  `test_descriptive_scorer.py`、`test_descriptive_capture.py` に回帰ケースを追加

### 2-8. 段階導入と完了条件

#### Phase A: 保存基盤と採点基準メモ

- [x] 注釈JSONの読込・保存・正規化を実装する。
- [x] 問題設定と両採点モードに採点基準メモを追加する。
- [x] 旧データ互換テストを通す。

#### Phase B: 回答メモ・コメント・保留タグ

- [x] 1枚ずつモードへ回答メモ・コメント・保留・タグの基本操作を実装する。
- [x] 自動保存、コメント履歴、入力中のショートカット抑止を実装する。
- [ ] 保留・タグによる絞り込みと保留間移動を実装する。
- 一覧モードへバッジと編集操作を実装する。

#### Phase C: 確認・描画・仕上げ

- 採点結果確認画面と保留レビューを統合する。
- コメントの答案描画と表示設定を実装する。
- 全テスト、型チェック、手動スモークテストを完了する。

機能全体の完了条件は以下とする。

- 3機能が1枚ずつ／一覧／確認画面で一貫して操作できる。
- 入力内容が画面移動、キャンセル、再起動で失われない。
- 既存の得点JSON形式と分析・Excel・R出力の結果を壊さない。
- 生徒向けコメントだけが設定に従って答案へ描画される。
- 旧データ互換を含む自動テストとmacOSでの手動確認が完了している。

### 2-9. 実装前に最終決定する項目

以下は実装を止める問題ではないため、初期案を採用して着手し、実機UI確認時に調整する。

- コメントの最大文字数: 初期案200文字。
- 教員用メモの最大文字数: 初期案2,000文字。
- タグ1件の最大長と件数: 初期案30文字・1回答10件。
- コメント描画位置が他の解答欄と重なる場合の優先順位。
- 小さい画面での注釈パネルの初期配置（右側または下側）。
- コメント履歴を最近使用順だけにするか、使用頻度順も追加するか。
