# ロードマップ（今後実装したいこと）

まだ着手していないが、実装したい機能・修正をここに記録する。
着手したら該当セクションの先頭に `[着手中]` を付け、実装が終わったら
このファイルから削除して構わない。

---

## 1. v0.1.0 リリース準備

マル之助としての最初の公開版 `v0.1.0` を作成する。別の機能修正を優先するため、
現時点では未着手とし、修正完了後に以下の順序で再開する。

### 1-1. 残存名称・ドキュメント監査

- ユーザー向け画面に旧アプリ名が残っていないことを確認する。
- README・DEVELOPMENT・要件書と現在の機能が一致していることを確認する。
- 削除済みのマーク採点機能や旧モードについて、現行機能の説明として残っている
  記述を削除または履歴であることが分かる表現へ直す。
- 「採点侍 SaitenSamurai」は、原作へのクレジット、後方互換API、履歴の説明など
  必要な箇所だけに残す。
- リポジトリURL、Issue URL、cloneコマンド、バッジが
  `takehikoyasuda/Marunosuke` を参照していることを確認する。

### 1-2. テストと手動動作確認

- 標準の非visualテストを実行し、すべて成功することを確認する。
- アプリ起動、PDF・画像読込、初期設定、名簿読込、記述採点、集計出力、
  セッション復元を実データで確認する。
- ウィンドウタイトル、ロゴ、アイコン、ログ名がマル之助の名称になっていることを
  macOS上で確認する。
- GitHub Actionsのテスト結果を確認する。

### 1-3. リリース文書

- `CHANGELOG.md` を追加し、`v0.1.0` を「マル之助としての最初のリリース」として
  記録する。
- 主な機能、原作との関係、既知の制限、動作環境、起動方法を記載する。
- GPL-3.0、`CREDITS.md`、`THIRDPARTYLICENSES.md` の参照が正しいことを確認する。

### 1-4. ブランチとリリース

- `main` へ統合済みの `refactor/descriptive-only` を、不要であることを確認して削除する。
- 旧 `feat/student-id-ocr` ブランチも、必要な履歴が `main` に含まれていることを
  確認してから整理する。
- バージョン定数が `0.1.0` であることを確認し、`v0.1.0` タグを作成する。
- GitHub Releaseを作成し、リリースノートとソースコードの取得方法を案内する。
- 必要に応じて、ローカルの作業フォルダ名を `Marunosuke` へ変更する。

### 1-5. 完了条件

- テストと主要な手動操作に問題がない。
- ユーザー向けの旧名称・古い機能説明が整理されている。
- 権利表示と原作者へのクレジットが維持されている。
- `v0.1.0` タグとGitHub Releaseが公開されている。

---

## 2. [着手中] 採点補助機能（採点基準メモ・教員用メモ・生徒コメント・採点保留）

> 進捗（2026-07-28）: Phase Aを開始。`descriptive_annotations.json` の
> 保存・読込・正規化基盤と、採点基準メモの設定・採点中編集・両採点モードでの
> 参照まで実装済み。Phase Bの「1枚ずつ」採点画面に、教員用メモ、
> 生徒向けコメントと履歴、保留、複数タグ、自動保存を実装済み。
> 次は一覧モードへの注釈編集・バッジ・フィルタ統合を行う。
>
> 操作改善（2026-07-28）: 数字キー採点の初期フォーカスを修正し、
> 「保留して次へ」ボタンを追加。画像フォルダ／PDF選択後は画像準備から
> 初期設定まで自動で進み、既存設定の初期化確認は各設定を始める前に行う。
>
> 方針変更（2026-07-29）: タグ機能は廃止する。既に保存されている `tags` は
> 移行期間中に安全に読み込めるようにしたうえで、UIから除去する。代わりに、
> 教員用メモにも生徒向けコメントと同様の履歴選択・挿入機能を追加する。

### 2-0. 目的と実装方針

記述式採点中の判断根拠を残し、似た答案を比較し、採点済み答案へ必要な
フィードバックを返せるようにする。対象は `descriptive_gui.py` の「1枚ずつ」
および「一覧（グリッド）」の両採点モードと、採点結果の確認画面、
`descriptive_renderer.py` の答案描画である。

既存の `descriptive_scores.json` は、CTT分析・Excel出力・R連携などが
`{画像ファイル名: {問題ID: 整数得点}}` を前提としている。この形式は変更せず、
追加情報は新しい `descriptive_annotations.json` に分離する。これにより、既存データ、
既存の分析処理、旧バージョンとの後方互換性を維持する。

実装は次の順序で進める。

1. 共通データモデルと保存処理
2. 採点基準メモ
3. 回答ごとのメモと一言コメント、および両方の履歴
4. 採点保留
5. 確認画面・答案描画・回帰テスト

### 2-1. データモデルと永続化

#### 問題設定（`descriptive_config.json`）

設問ごとの採点基準は既存の問題設定に任意キーとして追加する。

```json
{
  "version": 2,
  "questions": [
    {
      "id": "D1",
      "name": "記述1",
      "max_score": 5,
      "aspect": 1,
      "region": [100, 200, 500, 300],
      "rubric_memo": "結論2点、理由3点。単位不足は1点減点。"
    }
  ]
}
```

- `rubric_memo` はプレーンテキストとし、空文字を許可する。
- キーがない旧設定は空文字として扱う。読み込み時にファイルを書き換えない。
- 問題の追加・編集の両画面から変更できるようにする。
- 問題を削除する際は、対応する注釈とコメント履歴も削除対象として確認する。

#### 回答注釈（新規 `descriptive_annotations.json`）

```json
{
  "version": 1,
  "answers": {
    "001.jpg": {
      "D1": {
        "memo": "別解。後で他答案と比較する。",
        "comment": "理由の説明が不足しています。",
        "held": true,
        "tags": ["別解", "境界答案"]
      }
    }
  },
  "comment_history": {
    "D1": [
      "理由の説明が不足しています。",
      "単位を書きましょう。"
    ]
  }
}
```

- `memo` は教員用であり、答案画像・Excel・分析結果には出力しない。
- `comment` は生徒へ返す一言コメントで、答案画像へ出力する。
- `held` は採点保留状態。得点の有無とは独立させる。
- `tags` は旧バージョンとの読込互換のため当面許容するが、新規編集・保存の対象にはしない。
- `comment_history` は問題ごとに独立させ、重複を除き、最近使用した順に並べる。
  初期上限は50件とし、上限値は定数化する。
- すべて既定値（空文字、`false`、空配列）の回答は保存時に削除し、ファイルの肥大化を防ぐ。
- 注釈ファイルが存在しない場合は空データとして開始し、旧セッションをそのまま開けるようにする。
- 不正JSONからの復旧、アトミック保存、`.bak` からの復元は既存の
  `atomic_json_save()` / `load_json_safe()` を利用する。

#### 実装箇所

- `descriptive_scorer.py`
  - `DESCRIPTIVE_ANNOTATIONS_FILE` 定数を追加する。
  - `load_descriptive_annotations()` / `save_descriptive_annotations()` を追加する。
  - 旧ファイルや任意キー欠損を正規化する小さな純粋関数を用意する。
- `descriptive_gui.py`
  - `DescriptiveScorerGUI` が得点と注釈を同時に読み込み、各採点画面へ渡す。
  - 得点の保存タイミングと同じ箇所で注釈も保存する。
  - テキスト入力はフォーカス移動時と画面終了時に確定し、入力中は300〜500msの
    デバウンス付き自動保存を行う。クラッシュ時の損失を現在編集中の数文字に限定する。
  - 「キャンセル」は画面を閉じる意味とし、得点と同様に、それまでの注釈は保存する。
- `main_gui.py`
  - セッションの完全性確認では注釈ファイルを必須にしない。
  - 採点済み答案生成時に注釈を読み込み、描画関数へ渡す。

### 2-2. 採点基準メモ

#### UI仕様

- 記述問題設定画面の問題追加・編集欄に「採点基準メモ」を追加する。
- 1枚ずつの採点画面では答案画像の横に編集可能なメモ欄を常時表示する。
- 長文を想定して複数行・折り返し・縦スクロールに対応する。
- 採点領域を圧迫しないよう、初期表示は3〜5行程度とし、「折りたたむ／展開する」を用意する。
- 答案を見ながら随時追記できるようにし、入力停止後に自動保存する。
- 一覧モードでは上部バーから編集ダイアログを開き、各カードへ同じ文面を重複表示しない。

#### 受け入れ条件

- 設問ごとのメモを登録・編集・削除し、再起動後も復元できる。
- 旧 `descriptive_config.json` を警告なしで読み込める。
- 1枚ずつ／一覧の両モードで、同じ最新のメモを確認できる。
- 採点基準メモは採点済み答案には印字されない。

### 2-3. 回答ごとのメモ

#### UI仕様

- 1枚ずつモードの画像下部または右側に「教員用メモ」欄を配置する。
- 前後移動、未採点ジャンプ、フィルタ変更、画面終了の前に現在値をモデルへ反映する。
- 一覧モードでは、カードのメモアイコンから小さな編集ダイアログを開く。
- メモがあるカードはアイコンまたはバッジで識別できるようにする。
- 採点結果確認画面でも内容の閲覧・編集を可能にする。
- 生徒向けコメントと同様に、過去に入力した教員用メモを設問ごとの履歴として保存する。
- 履歴候補を選択して現在のメモ欄へ挿入できるようにする。既存内容を置換するか
  末尾へ追記するかを分かりやすく選べるようにする。
- 履歴候補の個別削除と、その設問の履歴全消去を可能にする。

#### 受け入れ条件

- 画像ファイル名と問題IDの組み合わせごとに独立して保存・復元できる。
- 画面移動直後や採点画面を閉じた直後にも内容が失われない。
- メモの有無で得点、進捗判定、分析、Excel出力、答案描画が変化しない。
- 教員用メモの履歴が設問間で混ざらず、再起動後も利用できる。

### 2-4. 一言コメント機能

#### UI仕様

- 1枚ずつモードに「生徒へのコメント」入力欄と履歴候補のコンボボックスを置く。
- 候補選択で入力欄へ設定でき、新しい文面は確定時にその問題の履歴へ追加する。
- 一覧モードでは各カードのコメントアイコンから選択・編集できるようにする。
- コメントありのカードにはバッジを表示する。
- 履歴候補の個別削除と、その問題の履歴全消去を可能にする。
- コメントは1回答につき1件とする。複数の指摘は改行または区切り文字で入力する。

#### 答案描画

- `draw_descriptive_on_image()` に回答注釈を任意引数として渡せるようにする。
- コメントは対象解答欄の直下を第一候補とし、用紙外にはみ出す場合は欄内下端へ配置する。
- 得点表示と重ならないよう、コメント用の矩形を先に決定してから得点の有効領域を調整する。
- 日本語フォント、改行、折り返し、出力倍率を考慮してフォントサイズを自動調整する。
- 長すぎて収まらない場合は、最小フォントまで縮小したうえで末尾を省略記号にする。
- コメントは黒または濃紺、半透明の白背景付きとし、採点記号との視認性を分ける。
- 描画詳細設定に `descriptive_show_comment`（既定値 `true`）を追加し、出力時に非表示を選べるようにする。
- コメント未設定の場合は従来とピクセル単位で同等の描画結果を維持する。

#### 受け入れ条件

- コメント候補は設問間で混ざらず、再起動後も使用できる。
- 自由入力したコメントが履歴へ追加され、重複登録されない。
- コメントの表示ON/OFFがセッション保存・復元後も維持される。
- 低解像度・高解像度の出力で、コメントが用紙外や対象欄外へ不自然にはみ出さない。
- 教員用メモと保留状態は答案画像へ印字されない。

### 2-5. 採点保留（タグ機能は廃止）

#### 状態と操作

- 「保留」は得点とは独立した状態にする。未採点のまま保留、仮得点を付けて保留、
  採点確定後に保留解除のすべてを許可する。
- 1枚ずつモードに「保留」トグルを置く。
- キーボードショートカット（候補: `h`）を設ける。ただし既存の得点入力、
  Entry/Textへの文字入力中には発火させない。

#### 一覧・絞り込み

- 問題一覧の進捗に「採点済み数」と「保留数」を併記する。
- 1枚ずつモードに「保留のみ」のフィルタを追加する。
- 一覧モードでは保留カードを橙色の枠とバッジで強調する。
- 「保留のみ」と「未採点のみ」を切り替えて絞り込めるようにする。
- 保留答案の間を前後移動できるようにする。
- 採点完了時に保留が残っていれば件数を示して確認する。ただし保存・終了自体は妨げない。

#### 受け入れ条件

- 得点変更や得点リセットで、保留状態が意図せず失われない。
- 問題設定から「この問題の採点をリセット」を実行する際は、得点だけを消すか、
  注釈も含めて消すかを明示的に選べる。
- 保留中でも仮得点は分析・Excel出力に従来どおり含める。保留情報自体は分析値を変えない。
- 保留状態と教員用メモは採点済み答案へ印字されない。

### 2-6. 採点結果確認画面との統合

- 既存の記述採点結果確認画面に、メモ・コメント・保留の状態を示す列またはバッジを追加する。
- 問題、得点、保留状態、コメント有無で絞り込めるようにする。
- 確認画面から得点と注釈を編集し、両JSONをアトミックに保存する。
- 保留答案を一括確認できる導線を問題一覧画面にも置く。
- ファイル名変更や画像の追加・削除で参照不能になった注釈は自動削除せず、ログへ警告する。
  将来の再関連付けに備え、明示的なクリーンアップ操作を別途検討する。

### 2-7. テスト計画

#### 単体テスト

- 注釈JSONの保存・読込・`.bak` 復旧・欠損キー補完。
- 空注釈の削除、教員用メモ・生徒コメント履歴の順序と上限、旧タグデータの読込互換。
- 旧問題設定・旧得点データを変更せず読み込めること。
- 保留数・採点済み数・フィルタ結果を算出する純粋関数。
- コメント描画の座標計算、折り返し、省略、表示ON/OFF。

#### GUIテスト

- 1枚ずつモードでの回答切替時の保存。
- 一覧モードのバッジ、編集ダイアログ、未採点・保留フィルタ。
- Text/Entry入力中に得点・保留ショートカットが誤発火しないこと。
- 問題設定画面での採点基準メモ編集と、採点画面への反映。
- 採点完了時の保留警告と、終了継続・キャンセルの両分岐。

#### 統合・回帰テスト

- アプリ再起動を挟んだ得点・注釈・コメント履歴の復元。
- 旧セッション、注釈なしセッション、破損注釈ファイルからの起動。
- CTT分析、Excel出力、R連携の数値が追加前と一致すること。
- コメントあり／なし、複数出力倍率、日本語長文での答案画像スナップショット。
- macOSのTk環境で、1枚ずつ／一覧／確認画面の手動スモークテスト。

追加するテストファイルの目安:

- `tests/test_descriptive_annotations.py`: データモデルと永続化
- `tests/test_descriptive_annotation_gui.py`: GUI状態遷移と保存
- `tests/test_descriptive_comment_rendering.py`: コメント描画
- 既存の `test_session_state.py`、`test_rendering_settings.py`、
  `test_descriptive_scorer.py`、`test_descriptive_capture.py` に回帰ケースを追加

### 2-8. 段階導入と完了条件

#### Phase A: 保存基盤と採点基準メモ

- [x] 注釈JSONの読込・保存・正規化を実装する。
- [x] 問題設定と両採点モードに採点基準メモを追加する。
- [x] 旧データ互換テストを通す。

#### Phase B: 回答メモ・コメント・保留

- [x] 1枚ずつモードへ回答メモ・コメント・保留の基本操作を実装する。
- [x] 自動保存、コメント履歴、入力中のショートカット抑止を実装する。
- [x] 教員用メモの履歴選択・挿入・削除を実装する。
- [x] タグUIを廃止し、旧タグデータを安全に読み込める移行処理を実装する。
- [ ] 保留による絞り込みと保留間移動を実装する。
- 一覧モードへバッジと編集操作を実装する。

#### Phase C: 確認・描画・仕上げ

- 採点結果確認画面と保留レビューを統合する。
- コメントの答案描画と表示設定を実装する。
- 全テスト、型チェック、手動スモークテストを完了する。

機能全体の完了条件は以下とする。

- 採点基準メモ、教員用メモ、生徒コメント、保留が1枚ずつ／一覧／確認画面で
  一貫して操作できる。
- 入力内容が画面移動、キャンセル、再起動で失われない。
- 既存の得点JSON形式と分析・Excel・R出力の結果を壊さない。
- 生徒向けコメントだけが設定に従って答案へ描画される。
- 旧データ互換を含む自動テストとmacOSでの手動確認が完了している。

### 2-9. 実装前に最終決定する項目

以下は実装を止める問題ではないため、初期案を採用して着手し、実機UI確認時に調整する。

- コメントの最大文字数: 初期案200文字。
- 教員用メモの最大文字数: 初期案2,000文字。
- コメント描画位置が他の解答欄と重なる場合の優先順位。
- 小さい画面での注釈パネルの初期配置（右側または下側）。
- 教員用メモ・生徒コメント履歴を最近使用順だけにするか、使用頻度順も追加するか。

---

## 3. 設問ごとの答案一覧・未採点レビュー

各設問について、採点前、採点途中、採点完了後のどの時点でも答案一覧を開けるようにする。
採点を開始していない状態でも、設定済みの採点領域を使って答案を一覧表示できることを
前提とする。

### 3-1. 一覧表示

- 設問ごとに、全答案をカードまたは行形式で一覧表示する。
- 各答案に氏名または学籍番号、答案画像、得点、採点状態、保留状態を表示する。
- 教員用メモと生徒向けコメントを同じカード内に表示する。
- 長いメモ・コメントは先頭部分だけを表示し、省略記号と展開操作を用意する。
- 一覧から得点、教員用メモ、生徒向けコメント、保留状態を編集できるようにする。

### 3-2. 絞り込み

- 「未採点のみ」をボタンまたはチェックボックス1操作で抽出できるようにする。
- 全件、未採点、採点済み、保留を分かりやすく切り替えられるようにする。
- 絞り込み中の件数を「未採点 12 / 全40」のように表示する。
- 採点状態が変わったら一覧と件数へ即座に反映する。

### 3-3. 完了条件

- 採点前・途中・完了後のすべてで一覧を開ける。
- 未採点答案だけを1操作で抽出できる。
- メモとコメントが一覧で確認でき、長文でも一覧性を損なわない。
- 一覧から行った変更が1枚ずつ採点画面と確認画面へ即座に反映される。

---

## 4. 採点中の正解画像・解答例表示

正解や模範的な答案を忘れたとき、採点画面から離れずに確認できる小さな参照枠を
設置する。正解画像は設問ごとに独立して保存する。

### 4-1. 正解画像の登録

- 手持ちの解答例画像やスクリーンショットをファイルから読み込めるようにする。
- 採点中の生徒答案から、模範にしたい範囲を切り取って登録できるようにする。
- 登録後に切り抜き範囲の調整、差し替え、削除ができるようにする。
- 元画像へのパスだけでなく、必要な切り抜き画像を採点データ側へ保存し、
  元ファイル移動後も表示できるようにする。

### 4-2. 採点画面での表示

- 1枚ずつ採点画面と一覧採点画面に、小さな正解画像枠を設置する。
- 初期状態では採点領域を圧迫しない大きさにし、クリックで拡大表示できるようにする。
- 表示／非表示を切り替えられ、設問を移動すると対応する正解画像へ切り替わる。
- 正解画像が未登録の場合は、登録方法を示す小さな案内だけを表示する。

### 4-3. 完了条件

- ファイル読込と生徒答案からの切り抜きの両方で登録できる。
- 採点中に1操作で確認・拡大できる。
- セッションを閉じて再開しても設問ごとの画像が復元される。

---

## 5. 画面サイズ・解像度への自動適応

Mac、外部ディスプレイ、解像度、表示倍率、Tkバージョンが異なっても、ウィンドウを
開いた時点ですべての主要操作へ到達できる共通レイアウト基盤を導入する。

### 5-1. 共通仕組み

- 画面の利用可能領域を取得し、初期ウィンドウサイズをその範囲内に収める。
- 固定ピクセルの最小サイズだけに頼らず、内容量と画面サイズから適切な幅・高さを決める。
- 内容が収まらない場合は、主要領域へスクロールを自動的に追加する。
- 決定・保存・キャンセルなどの重要ボタンは、常に見える領域へ固定する。
- `fit_window_to_content()` など既存の調整処理を整理し、全ダイアログで使える
  共通ユーティリティにする。
- ウィンドウサイズと位置を記憶し、画面外へ出た場合は安全な位置へ戻す。

### 5-2. 検証

- 小型ノート画面、標準解像度、高解像度、外部ディスプレイで確認する。
- macOSの表示倍率やTkのフォント差があっても、主要ボタンが隠れないことを確認する。
- GUIテストで、画面サイズごとの初期geometryとスクロール有無を検証する。

---

## 6. トップ画面の進捗・次操作ガイド

トップ画面だけで、準備、採点、確認、集計のどこまで完了し、次に何をすべきかが
分かるようにする。

### 6-1. 表示内容

- Step 1、問題設定、採点、採点確認、集計などを一連の進捗として表示する。
- 完了した段階にはチェックマークと完了色を表示する。
- 未完了、実行可能、実行中、要確認、完了を視覚的に区別する。
- 次に行う操作を1つ強調し、該当ボタンへ視線を誘導する。
- 採点は「3 / 10設問完了」「未採点答案12件」のように具体的な件数を表示する。
- 必須でない処理は「任意」と示し、完了判定を妨げないようにする。

### 6-2. 状態判定

- ファイルの存在だけでなく、設定内容と保存データの整合性を確認して完了判定する。
- データを初期化・削除した場合は、関連する後続ステップを未完了へ戻す。
- セッション復元直後にも正しい進捗を再計算する。

---

## 7. 複数ページ答案のサブフォルダ管理

試験全体の主フォルダの下にページ別サブフォルダを作り、そのフォルダ内の画像を
すべて対応ページとして扱える入力方式を追加する。

### 7-1. フォルダ構造

```text
試験フォルダ/
├── page-01/
├── page-02/
└── page-03/
```

- サブフォルダ名からページ番号を自動認識する。認識できない場合は割当画面を表示する。
- 各サブフォルダ内の対応形式の画像を、そのページの答案として一括登録する。
- ページごとに画像枚数、学籍番号、重複、不足を確認できるようにする。
- ページ番号の誤配置を後から修正できるようにする。
- 従来の単一フォルダ入力も維持し、既存利用者の操作を壊さない。

### 7-2. 採点・集計との連携

- 設問とページの対応を保存し、採点画面では対象ページだけを読み込む。
- 同一生徒の複数ページを学籍番号または確認済みの対応情報で関連付ける。
- ページ不足・重複・未関連付けがある場合は、集計前に明確な警告を表示する。
- 既存の「複数ページ統合」処理と役割を整理し、重複操作を減らす。

---

## 8. UIの簡素化・ウィンドウ数削減

画面上の情報量と別ウィンドウの往復を減らし、現在の作業に必要な操作だけを
段階的に見せる。

- 同じデータを扱う設定画面は、可能な範囲でタブ、サイドパネル、展開領域へ統合する。
- 確認だけの小さなダイアログは、トップ画面内のステータス表示やインライン通知へ置き換える。
- 一連の作業中に同時に開くToplevel数を抑え、親子関係と閉じる順序を明確にする。
- 詳細設定は初期状態で折りたたみ、日常的に使う操作を優先して表示する。
- ボタンの数、文言、配置、色の役割を全画面で統一する。
- 既存機能を削る前に利用頻度と代替導線を確認し、操作不能になる機能を作らない。

完了条件は、主要な採点フローで開くウィンドウ数とクリック数が削減され、
初見の利用者が次の操作を迷わず選べることである。

---

## 9. 不要機能・統計処理の削除と軽量化

オリジナルの SaitenSamurai から引き継いだ機能のうち、マル之助の記述式採点に
不要なものを整理する。特に統計処理、分析レポート、R連携と、それらの依存パッケージを
重点的に監査する。

### 9-1. 監査

- CTT分析、PDF分析レポート、Rエクスポート、統計グラフの利用状況と必要性を確認する。
- `scikit-learn`、`matplotlib`、`reportlab` などについて、統計処理以外の機能が
  依存していないかをコードとテストで調査する。
- Excel集計のうち、日常的な採点結果出力に必要な部分と高度な統計部分を分離する。
- 削除候補ごとに、コード量、依存容量、起動時間、ビルドサイズへの効果を記録する。

### 9-2. 削除方針

- 記述式採点、名簿、学籍番号、採点済み答案、基本的な点数集計は維持する。
- 高度な統計機能は、完全削除または明示的なオプション機能への分離を検討する。
- 機能を削除するときは、UI、設定、出力ファイル、依存パッケージ、テスト、文書を
  同じ変更単位で整理する。
- 過去セッションに削除済み設定が含まれていても、起動や採点継続を妨げないようにする。

### 9-3. 完了条件

- 通常インストール時の依存パッケージ数と容量が減っている。
- 起動時間と配布物サイズが改善している。
- 基本的な記述式採点・集計フローの結果が削除前と一致する。
- README、開発文書、ライセンス一覧が新しい依存関係と一致している。
