# 答案用紙デザイン要件（学籍番号OCR・ページ番号確認）

SaitenSamurai-mac の「学籍番号OCR」機能（手書きの学籍番号を読み取り、名簿と照合して確認する機能）と「ページ番号確認」機能（同じページ番号の答案だけをまとめたバッチに取り違えが混ざっていないかを確認する機能）を正しく動作させるために、答案用紙が満たすべき要件をまとめる。

## 経緯（設計変更の理由）

当初は学籍番号欄の四隅に専用の小さいマーカーを配置し、答案画像ごとにそれを検出してマス目の位置を割り出す方式を検証した。実際のスキャン画像で検証したところ、罫線・手書き文字とマーカーを区別するのが難しく、ユーザーが大まかにドラッグ選択する精度では実用に耐えないことが分かった（実測で許容誤差が±15〜20px＝1.5mm程度しかなく、画面上ではマウスカーソル1個分より狭い精度が必要だった）。

そこで方針を変更し、**学籍番号欄専用のマーカーは廃止**し、代わりに**用紙全体の四隅マーカー（既存の、信頼性の高い仕組み）を基準に、学籍番号欄の位置を「検出」ではなく「計算」する**方式にした。これにより答案画像ごとの検出処理・ユーザーの矩形選択が一切不要になる。

### さらなる経緯（v2: 色検出方式への変更）

上記の割合計算方式を実機で使ったところ、「切り出す部分（各桁のマス）が学籍番号記入欄からかなりずれる」という重大なバグが判明した。原因は、(1)欄全体の位置を割合設定という間接的な値から計算しており実際の印刷位置とのズレを検証できないこと、(2)欄全体の矩形を桁数で均等分割しているだけで、実際のマス目の印刷位置が均等でない場合にズレが蓄積すること、の2点。

そこで再度方針を変更し、**桁ごとのマスを赤色の枠線で印刷し、スキャン後に色空間（HSV）で検出する**方式にした。過去に撤回した「専用マーカー検出」との違いは、黒罫線・黒/青ペンの手書き文字と明確に区別できる色を使う点。これにより過去の失敗要因(1)（罫線・筆跡との誤認）を解消する。位置は「計算」ではなく「検出」に戻すが、**用紙全体の四隅マーカーの仕組み自体（用紙全体の傾き補正・全OMR機能の基盤）は一切変更しない**。学籍番号欄の位置決めだけを、四隅マーカー基準の割合計算に一切頼らない独立した仕組みに変更する。

---

## 色検出方式の要件（現行）

### 1. 学籍番号欄の配置（用紙デザイン側）

- **桁ごとに独立した赤枠正方形を印刷する**（欄全体を1個の枠で囲んで均等分割する方式は廃止）。色は `RGB(255, 0, 0)` の純赤を推奨。黒罫線・黒/青ペンの筆跡と色空間ではっきり区別できることを狙う。線幅は目安1.0〜1.5pt（細すぎると連結成分が途切れる、太すぎると内側の記入スペースを圧迫する）。
- **マス間の余白はマス幅の15〜20%以上**を確保する。画像処理側で「隣接マスの赤枠同士がくっついて1つの連結成分になる」ことを防ぐため、この余白確保が今回の変更の要点。
- 桁数分の赤枠正方形を横一列に等間隔で配置する。桁数が変わっても同じテンプレートマクロ（LaTeXの`tikz`の`\foreach`等）で生成できるようにする。
- **学籍番号欄は、タイトル等の文章の流し込みに影響されない絶対座標で配置すること**（LaTeXの`\paperwidth`・`\paperheight`基準、`textpos`や`tikz`の`overlay`など）。これは変更なし。
- **四隅コーナーマーカーの探索範囲（各コーナーから幅1%マージン+幅30%、高さ8%）は避けて配置すること**。既存のページ番号印字の要件と同じ制約。

### 2. アプリ側の検出方式

- 学籍番号欄の位置は、**答案画像全体から赤色を自動検出**して求める。四隅マーカーの位置・割合とは一切連動しない、独立したロジック（`main_src/id_area_color_detector.py`）。
- 自動検出（HSVでの赤色閾値処理＋連結成分解析）で、期待した桁数と一致する数の赤枠が見つかれば、それぞれを1桁分のマスとして採用する。
- **自動検出に失敗した場合のみ**（今回配布済みの赤枠なし用紙を使う場合や、印刷・スキャン品質の問題で検出できない場合）、教員が最初のサンプル画像に対して**1マスずつドラッグで手動指定**する（既存の記述式採点範囲指定と同じドラッグ選択UIを流用）。手動指定した矩形は画像全体に対する割合として保存し、以降そのバッチの全画像に同じ矩形を適用する。
- 教員は試験（用紙デザイン）ごとに1回だけ設定（自動検出の確認、または手動指定）すればよく、以降はその試験の全答案ページに使い回せる。

### 3. 今回配布済みの用紙について

- 赤枠を追加した新しいテンプレートは**次回以降の試験から適用**する。今回すでに配布・回収済みの答案用紙（赤枠なし）では自動検出が失敗することが想定されるが、その場合は上記の手動指定フォールバック、または既存の低信頼度手動修正フロー（`student_id_review_gui.py`）でカバーする。

### 4. スキャン解像度

- 300dpi程度を推奨（この推奨自体は変更なし）。カラースキャンが必須（赤色検出のため、グレースケール/白黒スキャンでは機能しない）。

---

## ページ番号の要件

複数ページにわたる答案（例: 1人3枚）を、同じページ番号の答案だけをまとめて1つのフォルダ/PDFにしてアプリに読み込む運用を前提とする。「このバッチが第何ページ分か」は運用上バッチ単位で自明だが、取り違え・混入がないかをアプリ側でも機械的に確認できるよう、以下の要件で用紙に印字する。

### 1. 経緯（黒四角ビットコード方式の廃止）

当初、右上に「P1/P2/P3」を黒四角のビットコードで表す方式を検討したが、実地検証で、この黒四角が既存の四隅コーナーマーカー検出（用紙の各コーナーから幅30%×幅8%の範囲を探索）の探索範囲と重なり、黒四角の方が真のコーナーマーカーより面積が大きいために誤検出され、ページ全体の補正が歪む不具合が発生した。これを踏まえ、**黒四角のビットコードによる自動検出方式は廃止**し、大きめの印刷数字＋教員による手動確認に変更した。

### 2. ページ番号の印字

- **大きめの数字一文字**（例:「2」）を、はっきり読める大きさ・太さで印刷する。ビットコードのような黒四角のパターンは使わない。
- **四隅コーナーマーカーの探索範囲を避けて配置すること**。各コーナーの探索範囲は用紙の端から「幅の1%余白＋幅の30%」を水平方向に、「幅の8%」を垂直方向に取っている（A4横210mmなら概ね両端から65mm以内が危険域）。**用紙横方向のほぼ中央（目安: 左端から65mm〜145mmの範囲）に置けば、縦位置を問わず4隅どのコーナーの探索範囲とも重ならず安全。**

### 3. アプリ側の確認方法

- 学籍番号OCRのような自動位置検出・自動OCRの仕組みは持たない。教員が「🔢 ページ番号確認」ツールで、最初の1枚に対して印刷されたページ番号の数字を**手動で矩形選択**すると、同じ矩形位置を全画像に適用してOCRし、多数決と異なるファイル（取り違えの疑い）を一覧表示する。
- 印刷された数字はOCRへの精度要求が低く（手書き学籍番号8桁のようなマス単位の精密な位置検出は不要）、多少ラフな矩形選択でも安定して認識できる。矩形が小さい対象でも選べるよう、選択ダイアログは拡大縮小（マウスホイール／＋－ボタン）に対応している。
- この確認結果は集計Excelには出力しない（今回はあくまで取り違え検知のための確認ツール）。

---

## 複数ページ答案の統合

1人の学生が複数ページ（例: 3枚、各ページに異なる設問）を提出する場合の運用と、アプリ側の対応。

### 1. 運用（バッチ分け）

Step1〜3（OMR/記述準備・採点・集計）のパイプライン自体はページ番号対応にしていない。**「同じページ番号の答案だけをまとめたバッチ」ごとに、別々の画像フォルダ・座標定義・正答データでStep1〜3を独立に実行する**運用とする（ページ1用・ページ2用・ページ3用でそれぞれ別の集計Excelができる）。異なるページ番号の答案が1つの画像フォルダに混在するケースには対応しない（座標テンプレートの自動判定・自動切替が必要になり複雑になりすぎるため、検討の上で見送った）。

### 2. 統合ツール

各バッチの集計Excelには学籍番号OCR機能により「学籍番号(確認済み)」列（教員が確認・修正済み）が入っている。この列をキーに、複数の集計Excelを1つに統合する「🔗 複数ページ統合」ツール（`main_src/multi_page_merger.py`）を用意した。

- 対象は「記述のみモード」の集計Excel（`001_student_summary.xlsx`）のみ。マーク＋記述モードの集計Excelは対象外（将来必要になれば別途対応）。
- 教員がページ順にExcelを追加すると、学籍番号ごとに各ページの設問列・合計を横に連結し、総合計・総配点・欠落ページ（一部のページだけ未提出の学生の警告表示）を算出した1つのExcelを出力する。
- 学籍番号OCRで確認されていない（「学籍番号(確認済み)」列がない）Excelは統合できずエラーになる。

---

## スコープ外・撤回した要件

以下は前回までの検討事項だが、今回の方式変更により不要になった:

- 学籍番号欄専用の四隅マーカー（黒）の追加
- マス目の罫線の角を切り上げて隙間を作る対応
- マーカーサイズ・隙間の比率に関する要件（1.5〜2mm等）
- 学籍番号欄全体を1個の矩形として割合設定から計算し、桁数で均等分割する方式（実機で位置ズレが発覚したため撤回。現行は桁ごと独立した赤枠の色検出に置き換え）
- 学籍番号欄の位置を割合(%)で教員が数値入力するダイアログ（現行は自動検出結果のプレビュー確認＋失敗時のみ手動ドラッグ指定に置き換え）
- 四隅コーナーマーカーの仕組み自体を廃止する案（マークシート採点全体に影響する変更のため見送り。四隅コーナーマーカーは維持し、学籍番号欄の位置決めだけをそれと独立させる）
- ページ番号を黒四角のビットコードで表す方式（コーナーマーカー検出との衝突により廃止）
- ページ番号の自動OCR・自動位置検出（教員による手動矩形選択＋確認ツールに変更）
- 異なるページ番号の答案が1つの画像フォルダに混在するケースへの対応（座標テンプレートの自動判定が必要になり複雑すぎるため見送り。バッチ分け運用が前提）
- マーク＋記述モードの集計Excelを対象にした複数ページ統合（記述のみモードのみ対応。将来必要になれば別途対応）
