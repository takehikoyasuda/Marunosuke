# 変更履歴

このファイルには、利用者に影響する主な変更を記録する。

## Unreleased

### 追加

- 記述式答案に特化した採点フローを整備した。
- 採点基準メモ、教員用メモ、生徒向けコメント、採点保留を追加した。
- メモとコメントの履歴、一覧画面からの注釈編集、保留・未採点などの絞り込みを追加した。
- 生徒向けコメントの答案描画と表示設定を追加した。
- 設問ごとの解答例画像について、ファイル登録、答案からの切り抜き、採点中の表示を追加した。
- 採点前・採点途中・採点後に利用できる答案の画像一覧・テキスト一覧を追加した。
- トップ画面に作業進捗と次の操作を案内する表示を追加した。
- 学籍番号OCR、名簿照合、複数ページ答案の統合、セッション保存・復元を整備した。
- 複数ページ答案について、同じ試験ページの複数PDF取込、取込元の追跡、
  ページ不足、重複、未関連付け、名簿外IDの監査基盤を追加した。
- 複数ページでレイアウトが共通の場合、学籍番号欄・氏名欄・解答欄の
  初期設定を全ページへ再利用できるようにした。
- 新規問題の標準名を「問題1」形式へ変更し、観点設定・表示を廃止した。
- 配点を必須入力として強調し、採点画面の問題一覧から変更しやすくした。
- 採点ステップから別の試験ページへ切り替えられる導線を追加した。
- トップ画面に複数ページの現在ページ、ページ別進捗、前後移動を表示した。
- 全ページの集計完了時に、学籍番号をキーとする統合サマリーを自動生成するようにした。
- トップ画面に全ページ集計ボタンを追加し、未集計ページの案内、ページ移動、
  統合Excelの生成と保存先表示を一つの操作で行えるようにした。
- 複数ページ案件のトップ画面に、作業中ページの状態（取込済み・採点中・
  採点完了・集計済み等）を表示するようにした。

### 変更

- macOS向けのフォント、ファイル操作、画面サイズ調整を改善した。
- テストCIをmacOS環境で実行するように変更した。
- 「答案ファイルを追加＆採点準備」ボタンの活性判定を、案件フォルダの
  実際の取込・準備状況（複数ページマニフェストや画像準備済みフォルダの有無）
  から判定するようにし、セッション復元後や「ページを管理」を直接使った
  場合でも正しく有効化されるようにした。
- 画像ファイルを直接使う案件では、複数ページ答案ダッシュボードを
  トップ画面に表示しないようにした。
- ページ切替失敗時のエラーメッセージを、技術的な例外文言ではなく
  分かりやすい日本語に変換して表示するようにした。
- 学生別得点と基本的な試験統計のExcel出力は維持しつつ、アプリ構成を軽量化した。
- プロジェクト名、アイコン、ログなどを「マル之助」に統一した。

### 削除

- マークシート採点機能と、その旧設定・サンプル・スクリーンショット生成処理を削除した。
- CTT分析、分析PDF、統計グラフ、R連携エクスポートを削除した。
- 統計レポート専用依存の `matplotlib` と `reportlab` を削除した。
- Windows版exeのビルドスクリプト、PyInstaller設定、リリースワークフロー、Windows用アイコンを削除した。

## 0.1.0

マル之助としての最初の公開版。リリース時に `Unreleased` の内容をこの項目へ移す。
